# MathForge · 架构设计（公开版摘要）

> 完整设计决策记录（D024-D026 等改向关卡）保留在作者本地 `DECISIONS.md`，本文件为可公开的架构摘要。

## 定位

考研数学一练习的垂直 Agent。封闭领域（数一考纲）让"记忆 / 计划 / 评测"可以脱离 LLM 幻觉、用符号系统 + 事件流 + 规则策略落地并量化。

## 三层分工（核心红线）

1. **结构选择层（LLM 可承担）**：面对无法符号化的考点，LLM 选择题型结构与表述——但产物必须过验证链，否则不放出。
2. **数学正确性（SymPy 承担）**：确定性模板族（`packs/*/families/*.py`）由 SymPy 构造参数化题面与答案，"构造即正确"，LLM 不写答案表达式（K1 教训：LLM 写不出随参数正确的 answer_expr）。
3. **判分与记忆（规则承担）**：填空 SymPy 等价判分；解答题用户三档自评（**机器不判步骤分**，红线）；事件流是唯一事实源，一切视图（掌握度/错题/薄弱模式/复习清单）都是它的投影。

## 数据流（一次练习闭环）

```
意图/知识点 → 两段式路由(包索引→确定性选择)
  → 包内确定性族（零 API，8 族）或 LLM+验证链（缓存优先）
  → 题目 + 溯源区（源题与变化 / 同族新实例）
  → 作答：填空(SymPy 判分) 或 解答题(折叠解答+三档自评)
  → 归因(四分类，可人工修正→override 事件，不重记作答)
  → 事件流 append → 掌握度投影(滑窗 N=5 加权,可重算)
  → P1-P6 数据化策略 → 下一题建议 / 复习清单(decayed 升序)
```

## 关键机制

| 机制 | 实现 | 为什么 |
|---|---|---|
| 事件流记忆 | `mem2.py`：attempt_events append-only + 三投影 | 唯一事实源；掌握度可离线重算（修 v1 N=1 过粗） |
| 策略数据化 | `policy2.py` P1-P6 阈值参数化（全局默认+包覆写） | 决策可回放可审计；修复 v1 P1 死代码（正式传前置掌握度） |
| 变式溯源 | `/v2/variant` 返回 provenance{源题摘要/变化点/一致性} | 演示可辩护："这题从哪来、改了参数几、知识点是否一致" |
| 科目包 | `packs/<subject>/`（kp DAG + families + 域段 skill + strategy） | 可扩展（准入硬门槛：可符号验证） |
| 模式蒸馏 | `/v2/patterns` 规则版（错题归因×kp 坑位） | 薄弱模式卡实时可解释 |

## 评测纪律（诚实口径）

- 方法：同数据、双臂/三臂对照、全缓存可重放、Wilson CI 披露、报告只陈述不预设。
- v1 六轮评测 M1：R1 45.0% → R6 31/40=77.5%（CI [0.625,0.877]，按知识点抽样）。
- S6 三臂（LLM 子集 n=16）：无 skill 68.8% / 全量 skill 43.8% / 分层 skill 56.2%（CI 披露，未预设结论；分域样本小待扩）。
- **口径声明**：v1 M1（知识点抽样）与 v2 M1（题型规格抽样）不可直接比较、不可拼同一条趋势线；引用必须带口径标签。
- 全量测试：234 passed（v1 基线 + v2 记忆层/决策层/路由/模拟器/两页 UI 流）。

## 目录

```
app.py / v2api.py     FastAPI：v1 端点 + /v2 两页 UI 支撑（intent/variant/review/patterns...）
mem2.py               事件流 + 掌握度(滑窗)/错题/薄弱模式投影
policy2.py/assembler.py/router.py/v2intent.py   决策/上下文组装/两段式路由/意图槽位
generate.py/families.py/verify.py   出题链（家族+LLM+验证）/ v1 确定性族 / SymPy 判分
packs/<subject>/       科目包：kp_graph(DAG) + families + examiner 域段 + strategy
ui2/                   两页 UI（今日=意图+复习卡驱动；足迹统计=只读）
tests/                 234 项（含评测纪律类测试）
```

## 合规

原始题库语料仅本地（`题集/`、`cache/`），不入库不随发行；仓库只含结构规格与原创参数样例。交互形态参考 MIT 项目 summer-checkin（仅信息架构，无代码）。
